# ws-text-lucene@1.1.0

Assistance à la génération de requête dans Istex Search

Génère une requête en syntaxe Lucene à partir d'une requête en langue naturelle (multilingue). C'est la syntaxe utilisée par Istex Search (et par l'API Istex).
